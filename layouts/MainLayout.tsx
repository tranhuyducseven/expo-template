import { View } from "react-native";

/**
 * 
 * @description use this layout to set bg image
 * @returns 
 */

export const MainLayout: IComponent = ({ children }) => {
  return <View>{children}</View>;
};
